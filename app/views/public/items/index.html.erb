<h2>商品一覧</h2>（全<%= @quantity %>件）
<% @items.each do |item| %>
<div>
  <%= link_to item_path(item.id) do %>
      <%= image_tag item.get_image(100,100) %>
  <% end %>

  <p><%= item.name %></p>
  <p><%= item.price %></p>
</div>
<% end %>
<%= paginate @items %>